import * as RTL from "@testing-library/react";

// Overview.test.ts
describe("# Overview", async () => {
  it.todo("renders scores (average: 1.5, sum: 3)");
});

// FiltersPanel.test.ts
describe("# FiltersPanel", async () => {
  it.todo("submits fields data correctly");
});
